<configuration>

    <!-- Appender pour le fichier -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file> <!-- Chemin du fichier de log -->
        <append>true</append> <!-- Pour ajouter au fichier existant -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender pour la console (facultatif) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuration du niveau de log -->
    <root level="INFO">
        <appender-ref ref="FILE" />
        <!-- Vous pouvez également activer la console si nécessaire -->
        <!-- <appender-ref ref="CONSOLE" /> -->
    </root>

</configuration>
